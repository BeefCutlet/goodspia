version: 0.0
os: linux
files:
  - source: /goodspia
    destination: /home/ubuntu/goodspia-source
    overwrite: yes

permissions:
  - object: /home/ubuntu/goodspia-source
    pattern: "**"
    owner: ubuntu
    group: ubuntu
    mode: 755

hooks:
  ApplicationStart:
    - location: scripts/deploy.sh
      timeout: 120
      runas: ubuntu